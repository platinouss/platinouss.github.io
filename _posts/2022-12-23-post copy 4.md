---
title:  "깃허브 블로그 test입니다."
excerpt: "테스트 내용"

categories:
  - etc
tags:
  - [Blog, jekyll, Github, Git]

toc: true
toc_sticky: true

date: 2022-12-23
last_modified_at: 2022-12-24
---

## 1. 안녕하세요 제목입니다.
안녕하세요 테스트 입니다 ~

## 2. 안녕하세요 제목입니다.
다음은 Java 코드입니다.

```java
package com.company;

import java.io.*;
import java.util.*;

public class Main {

    static int[] arr;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int N = Integer.parseInt(st.nextToken());
        int count = Integer.parseInt(st.nextToken());

        arr = new int[N];
        for (int i=0; i<N; i++) {
            arr[i] = Integer.parseInt(br.readLine());
        }

        Arrays.sort(arr);

        searchRange(N, count);
    }

    private static void searchRange(int N, int count) {
        int sValue = 1;
        int eValue = arr[N - 1] - arr[0];
        while (sValue < eValue) {
            int mid = (sValue + eValue + 1) / 2;
            if (getRouterCount(N, mid) < count) {
                eValue = mid - 1;
            } else {
                sValue = mid;
            }
        }
        System.out.println(sValue);
    }

    private static int getRouterCount(int N, int m) {
        int count = 1;
        int sIndex = arr[0];
        for (int i=1; i<N; i++) {
            if (arr[i] - sIndex >= m) {
                sIndex = arr[i];
                count++;
            }
        }
        return count;
    }
}
```

## 3. 안녕하세요 제목입니다.
- velog : [https://velog.io/@platinouss](https://velog.io/@platinouss)
